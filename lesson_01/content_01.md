# Заняття 1. Основи програмування та налаштування середовища

## Мета заняття

На першому занятті студенти:

* зрозуміють, що таке програмування та де використовується Python;
* встановлять Python і середовище розробки;
* навчаться запускати Python-код;
* створять перший `.py` файл;
* познайомляться з базовим синтаксисом Python.

# Частина 0. Знайомство

- Представлення викладача - хто він і чому він хоче вас навчати
- Чому люди роблять помилки?
- Важливість помилок у навчанні
- Кодекс і присяга студента:
  - я дозволяю собі робити помилки
  - я не засуджую себе і інших за помилки, що вони роблять
  - я обіцяю не критикувати себе і інших
  - я обіцяю давати дружні поради, якщо мене про це питають
  - я обіцяю задавати всі питання і буду пам’ятати що “поганих”, “дурних” чи “недоречних” питань не існує.

# Частина 1. Теорія 

## 1. Що таке програмування?

Програмування — це процес написання інструкцій для комп’ютера.

Комп’ютер не думає самостійно. Він виконує лише команди, які ми задаємо.

Приклад:

```python
print("Hello, World!")
```

Комп’ютер отримує команду: вивести текст на екран.

## 2. Інтерпретовані та компільовані мови

### Компілювані мови

Приклади:

* C
* C++
* Rust
* Go

Принцип роботи:

```text
Код → компілятор → машинний код → запуск
```

Плюси:

* висока швидкість;
* оптимізація.

Мінуси:

* потрібна компіляція.

### Інтерпретовані мови

Приклади:

* Python
* JavaScript
* PHP

Принцип роботи:

```text
Код → інтерпретатор → виконання
```

Python запускається рядок за рядком.

Плюси:

* швидка розробка;
* простий дебаг;
* інтерактивність.

Мінуси:

* повільніше виконання.

## 3. Чому Python?

Python — одна з найпопулярніших мов програмування.

Переваги:

* простий синтаксис;
* велика кількість бібліотек;
* кросплатформність;
* велика спільнота;
* підходить для:

  * web development;
  * automation;
  * testing;
  * data science;
  * scripting.

Приклад простоти:

```python
name = "Alex"
print(name)
```

## 4. Де використовується Python?

Python застосовується в:

* веброзробці
* тестуванні
* DevOps
* AI/ML
* data analysis
* automation scripts

Компанії:

* Google
* Netflix
* Instagram
* Dropbox

## 5. Python REPL (інтерактивна консоль)

REPL = Read → Eval → Print → Loop

Запуск:

```bash
python
```

Приклад:

```python
>>> 2 + 2
4

>>> print("Hi")
Hi
```

Навіщо:

* швидкі перевірки;
* навчання;
* експерименти.

## 6. Документація Python

Офіційна документація:

* [Python Documentation](https://docs.python.org/3/?utm_source=chatgpt.com)

Що там є:

* опис модулів;
* синтаксис;
* приклади.

Приклад пошуку:

* `python string methods`

## 7. Що таке IDE?

IDE = Integrated Development Environment

IDE допомагає:

* писати код;
* запускати програми;
* дебажити;
* форматувати код.

Популярні IDE:

### VS Code

* легкий;
* безкоштовний;
* багато extensions.

[VS Code](https://code.visualstudio.com/?utm_source=chatgpt.com)

### PyCharm

* потужна Python IDE;
* хороший debugger.

[PyCharm](https://www.jetbrains.com/pycharm/?utm_source=chatgpt.com)

# Частина 2. Практика

## 1. Встановлення Python

Завантажити:

[Python Downloads](https://www.python.org/downloads/?utm_source=chatgpt.com)

Під час встановлення Windows:

✅ Add Python to PATH

Перевірка:

```bash
python --version
```

Результат:

```bash
Python 3.12.0
```

## 2. Встановлення VS Code

Завантажити:

[Download VS Code](https://code.visualstudio.com/?utm_source=chatgpt.com)

Extensions:

* Python
* Pylance

## 3. Створення першого проєкту

Структура:

```text
lesson_01/
    main.py
```

## 4. Перший Python-скрипт

Створити файл:

`main.py`

Код:

```python
print("Hello, World!")
print("My first Python program")
```

Запуск:

```bash
python main.py
```

Результат:

```bash
Hello, World!
My first Python program
```

## 5. Перші змінні

**Змінна** - це іменована область пам'яті для збереження даних. Вона складається з **імені** та **значення**.

#### Особливості змінних у Python:
- **Динамічна типізація** - тип визначається автоматично
- Не потрібно **заздалегідь оголошувати** змінні
- **Можна змінювати тип** (але це не рекомендується)

#### Створення змінних:
```python
age = 18                   # Ціле число (int)
name = "Stephen"           # Рядок (str)
is_student = True          # Булеве значення (bool)

# А за таким символом приховуються коментарі. 
# Вони ні на що не впливають але полегшують розуміння коду
```

ЗВЕРНІТЬ УВАГУ
* для присвоєння значення змінній використовують знак `=`
* для порівняння значень або змінних використовують `==`

```python
name = "Oleksandr"
age = 30
is_student = True
```

Вивід:

```python
print(name)
print(age)
print(is_student)
```

#### Правила іменування змінних:
- ✅ Використовуйте **англійські** слова
- ✅ **Маленькі літери** з підкресленнями: `user_name`, `total_price`
- ✅ **Описові імена**: `age` замість `a`, `student_count` замість `sc`
- ❌ Не починайте з цифр: `2name` неправильно
- ❌ Не використовуйте спеціальні символи: `user-name` неправильно

## 6. Прості математичні операції

Python підтримує всі основні математичні операції:

```python
a = 10
b = 5

# Основні операції
addition = a + b        # Додавання: 15
subtraction = a - b     # Віднімання: 5
multiplication = a * b  # Множення: 50
division = a / b        # Ділення: 2.0

# Додаткові операції
power = a ** b          # Піднесення до степеня: 100000
```

## 7. Відступи (Indentation)

**Відступ** - це пробіли на початку рядка коду. У Python відступи **дуже важливі** - вони визначають структуру програми!

#### Правила відступів:
- Використовуйте **4 пробіли**
- **Будьте послідовні** в усьому коді
- Відступи позначають **блоки коду**

## 8. Вивід даних через print()

Функція `print()` виводить дані на екран:

```python
print("Hello, World!")                    # Вивід тексту
```

# Домашнє завдання

## Обов’язково:

### 1. Встановити:

* Python
* VS Code

### 2. Створити проєкт:

```text
my_first_project/
```

### 3. Створити або скопіювати файл `homework_1.py`

Вирішити усі 10 завдань, що описані у файлі

### 4. Самонавчання

Для самонавчання, якщо ви зовсім не впевнені у своїх силах є файли selflearning, 
що створені для дуже простих операцій і допоможуть потренуватися вам з програмуванням.
Перевірити правильність можна запустивши tests_selflearning.py

# Оберіть собі проект

Частиною навчання є створення власного проекту. 
У папці [practical](../practical) є три проекти на вибір. 
Доки іде навчання ви можете реалізувати один з них. 


# Підсумок заняття

Після заняття студент вміє:

* встановлювати Python;
* працювати з VS Code;
* запускати Python-код;
* використовувати REPL;
* створювати файли `.py`;
* писати прості програми.

## Питання для самоперевірки

1. Що таке Python?
2. Чим відрізняється інтерпретатор від компілятора?
3. Що таке REPL?
4. Як перевірити версію Python?
5. Як запустити файл Python?
